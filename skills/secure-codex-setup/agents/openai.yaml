interface:
  display_name: "Secure Codex Setup (legacy)"
  short_description: "Compatibility alias for Codex Safe Setup"
  default_prompt: "Use $codex-safe-setup to handle this existing Secure Codex Setup request."
policy:
  allow_implicit_invocation: false
