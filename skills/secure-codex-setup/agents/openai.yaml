interface:
  display_name: "Secure Codex Setup"
  short_description: "Audit and harden local Codex permissions safely"
  default_prompt: "Use $secure-codex-setup to audit my current Codex permissions and install a safer verified profile."
