---
name: secure-codex-setup
description: Deprecated compatibility alias for Codex Safe Setup 0.1.1 users. Use only when an existing prompt names secure-codex-setup; all new work should use codex-safe-setup.
---

# Secure Codex Setup compatibility alias

This skill was renamed to `codex-safe-setup` in version 0.1.2 so the skill, plugin, marketplace, and repository share one name.

Immediately load and follow `$codex-safe-setup` for assessment, installation, upgrade, verification, or rollback. Do not use scripts from an older standalone `secure-codex-setup` copy. If both names are installed outside the plugin cache, preserve the old folder as a recoverable backup outside skill discovery, refresh the `codex-safe-setup` plugin, and start a new task.
